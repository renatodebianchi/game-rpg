namespace GameRpg.Characters
{
    /// <summary>Which band a survival stat (hunger/sanity) currently falls into.</summary>
    public enum SurvivalThresholdLevel
    {
        Normal,
        Alert,
        Critical
    }
}
